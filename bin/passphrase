#!/usr/bin/env python3

import random
import string

def passphrase(chars = string.ascii_uppercase + string.digits, n=10):
  return ''.join(random.choice(chars) for _ in range(n))

print(passphrase(n=12))
